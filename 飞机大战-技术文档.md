# 飞机大战 · 网页小游戏 技术文档（Solo Builder 版）

> 本文档面向 AI / 单人开发者，目标是读完即可直接动工。文档已固化所有玩法、视觉、架构、数值与验收标准。请在文档基础上提出修改建议，最终版本会作为 Builder 的唯一指令源。

---

## 一、项目概述

### 1.1 项目定位
- 一款运行于现代浏览器（Chrome/Edge/Safari/Firefox 最新版）的 **HTML5 Canvas 纵向卷轴飞行射击游戏**（STG）。
- 单文件可部署：最终交付 `index.html` + 资源目录（或全部内联的 `game.html`）。
- 桌面浏览器为主适配目标，移动端浏览器可玩（虚拟摇杆）。

### 1.2 核心特色（差异化卖点）
1. **科技霓虹风**：深空黑底 + 青/紫/品红霓虹高亮 + 网格扫描线 + 全程粒子。
2. **弹幕多样化**：融合雷电式直线弹幕、东方式花型弹幕、怒首领蜂式贴脸擦弹。
3. **三大 Boss 战**：每个章节末出现巨型 Boss，多阶段、多弹幕模式切换、有弱点机制。
4. **武器自由切换**：主武器（3 种）+ 副武器（2 种）+ 大招（清屏炸弹）。
5. **局外成长**：局内掉落图纸，解锁更强战机/武器（存档于 localStorage）。
6. **Boss Rush 模式**：通关后解锁，连战所有 Boss。

### 1.3 玩法参考（市面爆火作品）
| 参考作品 | 借鉴点 |
|---|---|
| 《雷电 V》 | 直线主炮 + 锁定副武 + 全屏炸弹 |
| 《东方Project》 | 花型/环形/螺旋弹幕、擦弹高伤判定 |
| 《怒首领蜂》 | 贴脸高伤、Hyper 模式、连击数 |
| 《太空侵略者》 | 阵列推进的基础节奏 |
| 《微信飞机大战》 | 上手简单、道具即时反馈、社交分数 |
| 《彩京 1945》 | 兵器化 Boss、阶段切换 |

---

## 二、技术栈与部署

### 2.1 技术选型
- **渲染**：原生 HTML5 Canvas 2D Context（不使用 WebGL，降低 Builder 复杂度）。
- **语言**：原生 JavaScript（ES2020+），无框架依赖。
- **构建**：零构建。可直接 `file://` 打开运行；如需模块化用 `<script type="module">`。
- **存档**：`localStorage` 序列化 JSON。
- **音效**：Web Audio API（合成音 + 短音效，避免外部音频文件依赖）。
- **字体**：Google Fonts `Orbitron`（标题）+ `Rajdhani`（UI），网络加载失败回退 `monospace`。

### 2.2 性能目标
- 目标 60 FPS（1280×720 逻辑分辨率，CSS 缩放适配）。
- 同屏实体上限：玩家子弹 200、敌弹 600、粒子 800。
- 使用对象池（Object Pool）复用子弹与粒子。

### 2.3 适配与分辨率
- 逻辑分辨率：**720×1280**（纵向）。
- Canvas 通过 CSS 缩放至视口，保持 9:16 比例，左右留黑（桌面）或全屏（移动）。
- 移动端提供虚拟摇杆 + 自动开火开关。

---

## 三、游戏设计总览

### 3.1 游戏循环
```
标题界面 → 战机/武器选择 → 关卡 1-1 … 1-3 → Boss 1
        → 关卡 2-1 … 2-3 → Boss 2
        → 关卡 3-1 … 3-3 → Boss 3（最终）
        → 通关结算 → 解锁 Boss Rush / 更高难度
```

### 3.2 三种主武器（玩家可切换）
| 武器 | 名称 | 特性 | 适合场景 |
|---|---|---|---|
| A | 等离子激光 | 直线穿透、持续高伤、射程无限 | Boss 弱点、直线阵列 |
| B | 散射弹幕 | 扇形 5~7 发、范围广、单发伤害低 | 杂兵海 |
| C | 追踪导弹 | 自动锁定最近 3 目标、伤害中等 | 移动快的小型敌机 |

### 3.3 副武器与道具
- **副武器**：投掷型集束炸弹（范围爆炸）。
- **大招**：清屏电磁脉冲（EMP），3 秒内敌弹变慢并造成伤害，每局限充能（击杀回充）。
- **道具掉落**：
  - `P` 火力升级（+1 级，上限 5）
  - `B` 炸弹补充（+1）
  - `S` 护盾（吸收 1 次伤害）
  - `H` 回血（+1 命）
  - `C` 充能（大招槽 +30%）
  - `Blueprint` 图纸（局外解锁用，稀有）

### 3.4 擦弹与连击（差异化核心机制）
- 玩家判定点为飞机中心 **3×3 像素**点（非整架飞机）。
- 敌弹擦过判定点外圈（半径 20px）但未命中时，触发 **擦弹**：得分 +50，大招槽 +1%，画面边缘高亮闪烁。
- 连击：连续不被击中每击杀一个敌人 +1 连击，断连后清零。连击越高得分倍率越高（×1.0 → ×2.0）。

### 3.5 难度分级
| 难度 | 敌机 HP | 弹速 | 弹密度 | 擦弹窗口 |
|---|---|---|---|---|
| Easy | ×0.7 | ×0.8 | ×0.6 | 放宽 |
| Normal | ×1.0 | ×1.0 | ×1.0 | 标准 |
| Hard | ×1.3 | ×1.2 | ×1.4 | 收紧 |
| Lunatic | ×1.6 | ×1.4 | ×1.8 | 极严 |

---

## 四、美术与视觉规范（科技风）

### 4.1 配色方案（CSS 变量）
```css
--bg-deep:    #05060d;   /* 深空黑 */
--bg-grid:    #0d1430;   /* 网格线 */
--neon-cyan:  #00f0ff;   /* 主霓虹青 */
--neon-magenta:#ff00d4;  /* 副品红 */
--neon-purple:#9d00ff;   /* 紫 */
--neon-yellow:#ffe600;   /* 警示黄 */
--hud-white:  #e0f7ff;   /* HUD 文字 */
--danger-red: #ff2a4d;   /* 危险/Boss HP */
--ok-green:  #00ff9c;   /* 命中/回血 */
```

### 4.2 视觉风格规则
1. **背景**：黑色底 + 移动星点（3 层视差）+ 网格扫描线（缓慢下移）+ 偶尔扫过的能量带。
2. **战机**：几何拼接 + 外发光（`shadowBlur`），科技线条而非写实。
3. **子弹**：所有子弹带 `shadowBlur` 发光，主色随阵营区分（玩家青、敌弹品红/黄）。
4. **UI**：六边形 / 切角矩形面板，半透明 + 边框发光，等宽数字字体。
5. **粒子**：爆炸为放射状彩色粒子 + 冲击波环；命中为短火花。
6. **后处理**：画面震动（Screen Shake）、闪白（Hit Flash）、慢动作（Boss 死亡时 0.3 秒 slowmo）。

### 4.3 资源规范
- 所有战机/敌机/Boss 用 **Canvas 程序化绘制**（路径 + 渐变），不依赖图片，保证体积与可维护性。
- 字体：在线加载 Orbitron + Rajdhani，回退 monospace。
- 音效：Web Audio 合成（方波/锯齿波 + 包络），无外部音频文件。

---

## 五、核心系统架构

### 5.1 顶层架构图
```
┌─────────────────────────────────────────────┐
│                Game (主控制器)                │
│   状态机：TITLE/SELECT/PLAYING/PAUSE/OVER    │
└─────────────────────────────────────────────┘
        │           │           │           │
   ┌────┴───┐  ┌────┴───┐  ┌────┴───┐  ┌────┴────┐
   │ Input  │  │ Scene  │  │ Entity │  │  Render │
   │  Mgr   │  │  Mgr   │  │  Mgr   │  │   Mgr   │
   └────────┘  └────────┘  └────────┘  └─────────┘
                       │
   ┌──────────┬────────┴────────┬──────────┐
   │  Player  │  Enemies+Boss  │ Bullets   │
   │  System  │    System      │  System   │
   └──────────┘────────────────┴──────────┘
   ┌──────────┬────────────────┬──────────┐
   │ Particle │     Audio      │   UI     │
   │  System  │     System     │  System  │
   └──────────┘────────────────┴──────────┘
```

### 5.2 游戏主循环（固定时间步长）
```javascript
const FIXED_DT = 1000 / 60;  // 16.67ms
let accumulator = 0, lastTime = performance.now();

function frame(now) {
  let frameTime = now - lastTime;
  lastTime = now;
  if (frameTime > 250) frameTime = 250; // 防止卡顿后大跳
  accumulator += frameTime;
  while (accumulator >= FIXED_DT) {
    update(FIXED_DT / 1000);   // 逻辑更新（秒）
    accumulator -= FIXED_DT;
  }
  render();                    // 每帧渲染
  requestAnimationFrame(frame);
}
```

### 5.3 状态机
```
TITLE ──start──> SELECT ──launch──> PLAYING ──pause──> PAUSE
                    ▲                  │                │
                    │                  │ die             │
                    │                  ▼                │
                    │                OVER ──retry──────┘
                    └────── menu ──────┘
```

---

## 六、详细模块设计

### 6.1 实体系统（Entity Component 思想的轻量实现）
所有游戏对象继承 `Entity` 基类：
```javascript
class Entity {
  constructor(x, y, r) {
    this.x = x; this.y = y;       // 位置
    this.r = r;                   // 碰撞半径
    this.vx = 0; this.vy = 0;     // 速度
    this.alive = true;
    this.tag = '';                // 'player'|'enemy'|'boss'|'bullet_p'|'bullet_e'|'item'|'particle'
  }
  update(dt) {}
  render(ctx) {}
  hit(other) {}                   // 碰撞回调
}
```

碰撞检测采用 **空间网格哈希**（Spatial Hash Grid）优化，避免 O(n²)。

### 6.2 玩家系统（Player）
```javascript
class Player extends Entity {
  constructor() {
    super(W/2, H - 100, 4);       // 判定点极小（核心特色）
    this.weaponType = 'A';        // A/B/C
    this.power = 1;               // 1~5
    this.hp = 3;
    this.maxHp = 3;
    this.bombs = 3;
    this.empCharge = 0;           // 0~1
    this.shield = 0;
    this.invincible = 0;          // 无敌帧（秒）
    this.combo = 0;
    this.grazeWindow = 20;        // 擦弹半径
  }
}
```
- **移动**：键盘 WASD/方向键；鼠标按住跟随；移动端虚拟摇杆。按住 Shift 进入「精准模式」（速度减半，显示判定点）。
- **开火**：自动开火（按住空格或始终开火），不同武器射速不同。
- **死亡**：失去 1 命，火力降 1 级，1.5 秒无敌，全屏清弹。
- **重生**：HP 归 0 进入 OVER 状态。

### 6.3 敌机系统（Enemy）
```javascript
class Enemy extends Entity {
  constructor(type, x, y) {
    super(x, y, ENEMY_DATA[type].r);
    this.type = type;
    this.hp = ENEMY_DATA[type].hp;
    this.firePattern = ENEMY_DATA[type].fire;  // 'straight'|'aimed'|'spread'|'spiral'
    this.score = ENEMY_DATA[type].score;
    this.behavior = ENEMY_DATA[type].behavior; // 移动脚本
  }
}
```

#### 敌机类型表
| type | 名称 | HP | 移动 | 开火 | 分数 |
|---|---|---|---|---|---|
| `scout` | 侦察机 | 1 | 直线下 | 不开火 | 100 |
| `gunner` | 炮击机 | 3 | S形 | 瞄准弹 | 200 |
| `tank`  | 重装机 | 8 | 慢速横移 | 散射 3 | 400 |
| `weaver`| 编织者 | 4 | 正弦 | 螺旋 | 300 |
| `rusher`| 突击机 | 2 | 冲撞 | 不开火 | 150 |
| `turret`| 炮台 | 6 | 静止（贴边） | 多向 | 500 |

#### 敌机生成脚本（关卡数据驱动）
关卡用时间轴 JSON 描述：
```json
{
  "stage": "1-1",
  "events": [
    { "t": 1.0, "type": "spawn", "enemy": "scout", "x": 0.3, "y": -0.1, "count": 5, "interval": 0.3 },
    { "t": 3.5, "type": "spawn", "enemy": "gunner", "x": 0.5, "y": -0.1 },
    { "t": 6.0, "type": "warning", "msg": "WARNING" },
    { "t": 8.0, "type": "boss", "id": "boss_1" }
  ]
}
```

### 6.4 弹幕系统（Bullet）
- **子弹对象池**：预分配 1000 个，`active` 标志位复用。
- **弹幕类型**：
  - `straight`：直线
  - `aimed`：瞄准玩家当前位置
  - `spread`：扇形 N 发
  - `ring`：环形 N 发
  - `spiral`：旋转发射（每帧角度递增）
  - `curve`：贝塞尔/加速度曲线
  - `laser`：激光（带预警线 0.5s）
- **弹幕模式（Boss 阶段切换）**：
  - 阶段 1 (HP 100%~70%)：扇形散射 + 瞄准弹交替
  - 阶段 2 (HP 70%~35%)：双层旋转环 + 螺旋
  - 阶段 3 (HP 35%~0%)：狂暴弹幕地狱（密度 ×2）

### 6.5 Boss 系统（核心特色）

```javascript
class Boss extends Entity {
  constructor(id) {
    super(W/2, -200, 60);
    this.id = id;
    this.maxHp = BOSS_DATA[id].hp;
    this.hp = this.maxHp;
    this.phase = 1;
    this.patternIndex = 0;
    this.patternTimer = 0;
    this.weakPoints = BOSS_DATA[id].weakPoints; // [{x,y,r,active}]
    this.entryDone = false;
    this.state = 'enter';  // enter|fight|stunned|dead
  }
}
```

#### 三大 Boss 设计
| ID | 名称 | 主题 | HP | 弱点 | 阶段弹幕 |
|---|---|---|---|---|---|
| `boss_1` | 钢铁哨卫 | 机械守门人 | 2000 | 胸口能量核心（每 8 秒暴露 2 秒） | 散射 → 环形 → 激光扫射 |
| `boss_2` | 深渊母舰 | 战舰型 | 4000 | 两侧炮台（先摧毁才能伤主体） | 双侧交叉 → 子机群 → 全屏弹幕地狱 |
| `boss_3` | 终焉协奏 | 形态切换 Boss | 6000 | 形态切换时短暂暴露 | 三形态：均衡/狂攻/守御，每形态独立弹幕组 |

#### Boss 战流程
1. **入场**：从屏幕上方缓慢进入 + 警告条 + 专属 BGM（合成器音色切换）。
2. **战斗**：按阶段切换弹幕模式（每模式持续 6~10 秒后切换）。
3. **弱点暴露**：触发条件后核心变红、闪烁，期间受到伤害 ×3。
4. **破阶段**：HP 跨过阶段阈值时，全屏清弹 + 短暂硬直 + 屏幕震动。
5. **死亡**：慢动作 0.3s + 多段爆炸（按部件分别爆炸）+ 战利品掉落。

### 6.6 道具系统（Item）
- 自动下落，玩家接触即拾取。
- 火力满级时 `P` 转化为额外分数。
- `Blueprint` 为稀有掉落，解锁局外战机皮肤或新武器。

### 6.7 粒子系统（Particle）
```javascript
class Particle {
  // 复用池：粒子总数上限 800
  // 类型：spark（命中）/ explosion（爆炸）/ trail（尾迹）/ shockwave（冲击波环）
}
```
- 命中：4~8 个短寿命火花。
- 爆炸：12~20 个放射粒子 + 1 个冲击波环（扩散+淡出）。
- 战机尾焰：每帧 1~2 个低寿命粒子，颜色随武器变化。
- Boss 死亡：30~50 个粒子 + 多重冲击波。

### 6.8 音效系统（Audio）
- 全部用 Web Audio API 实时合成：
  - 射击：短促方波，频率随武器变化。
  - 命中：白噪声 + 短包络。
  - 爆炸：低频锯齿 + 噪声混响。
  - 擦弹：高频短"叮"声。
  - Boss 警告：低频脉冲 + 上升音阶。
  - BGM：可选合成器循环（Builder 可先用静默，留接口）。
- 全局音量控制 + 静音开关（存 localStorage）。

### 6.9 UI 系统
- **HUD**：左上 HP（六边形图标 ×3）、左下武器图标 + 火力等级、右下炸弹数、右上分数 + 连击倍率、顶部 Boss HP 条（仅 Boss 战显示）。
- **菜单**：标题、开始、选关、设置（音量/键位）、Boss Rush。
- **结算**：通关分数、最大连击、用时、评级（S/A/B/C）。
- **暂停**：半透明遮罩 + 继续/重开/退出。

---

## 七、关卡与数值设计

### 7.1 关卡结构（3 章 × 3 小关 + Boss）
| 章节 | 主题 | 视觉调色 | Boss |
|---|---|---|---|
| 第 1 章 | 近地轨道 | 青/蓝 | 钢铁哨卫 |
| 第 2 章 | 深空残骸带 | 紫/品红 | 深渊母舰 |
| 第 3 章 | 异星要塞 | 红/橙 | 终焉协奏 |

### 7.2 数值平衡
- 玩家主炮伤害：基础 10/发，每级 +2。
- 普通敌机 HP：1~10。
- Boss HP：2000~6000（Normal）。
- 命中率/弹药密度按难度系数缩放。

### 7.3 局外成长
- localStorage 存档：最高分、解锁状态、累计图纸。
- 解锁内容：3 种战机皮肤、隐藏武器 D（双管激光）、Boss Rush 模式、Lunatic 难度。

---

## 八、文件结构

```
飞机大战/
├── index.html              # 入口，加载所有脚本
├── style.css               # 全局样式 + 字体 + Canvas 容器
├── README.md               # 操作说明
└── src/
    ├── main.js             # 程序入口、Game 类、主循环
    ├── config.js           # 常量、分辨率、颜色、难度系数
    ├── core/
    │   ├── entity.js      # Entity 基类
    │   ├── pool.js         # 对象池
    │   ├── spatial.js      # 空间网格哈希
    │   ├── input.js        # 输入管理（键鼠/触屏）
    │   └── state.js        # 状态机
    ├── systems/
    │   ├── player.js      # Player 类
    │   ├── enemy.js       # Enemy 类 + 敌机表
    │   ├── bullet.js      # Bullet + 子弹池 + 弹幕模式
    │   ├── boss.js        # Boss 类 + Boss 表
    │   ├── item.js        # Item 类
    │   ├── particle.js    # Particle + 粒子池
    │   └── collision.js   # 碰撞分发
    ├── data/
    │   ├── stages.js      # 关卡时间轴 JSON
    │   ├── enemies.js     # ENEMY_DATA
    │   └── bosses.js      # BOSS_DATA
    ├── render/
    │   ├── sprites.js     # 程序化绘制（战机/敌机/Boss）
    │   ├── background.js  # 视差星空 + 网格
    │   ├── fx.js          # 屏震/闪白/慢动作
    │   └── ui.js          # HUD/菜单绘制
    ├── audio/
    │   └── synth.js       # Web Audio 合成
    └── util/
        ├── math.js        # 向量、角度、距离
        ├── storage.js     # 存档读写
        └── easing.js      # 缓动函数
```

> 若 Builder 倾向单文件交付，可将 `src/**` 合并到 `game.js` 一个文件，按上述顺序拼接即可。

---

## 九、开发阶段计划（建议按此顺序实现）

### 阶段 0：骨架（0.5 天）
- Canvas 初始化、主循环、固定时间步长、状态机空实现。
- 输入管理（键盘 + 鼠标 + 触屏）。
- 背景星空 + 网格。

### 阶段 1：玩家与基础射击（0.5 天）
- Player 移动 + 三种主武器开火。
- 子弹池、基础碰撞、敌弹擦弹。

### 阶段 2：敌机与弹幕（1 天）
- 6 种敌机 + 移动行为。
- 弹幕模式：straight/aimed/spread/ring/spiral。
- 关卡时间轴驱动生成。

### 阶段 3：Boss 战（1 天）
- Boss 入场、阶段切换、弱点、死亡过场。
- 实现 Boss 1 全套，Boss 2/3 复用框架填数据。

### 阶段 4：道具与成长（0.5 天）
- 道具掉落 + 拾取。
- 大招 EMP、护盾、连击系统。

### 阶段 5：UI 与音效（0.5 天）
- HUD、菜单、结算、暂停。
- 音效合成 + 音量开关。

### 阶段 6：打磨（1 天）
- 粒子细节、屏震、慢动作、闪白。
- 难度平衡、性能优化、移动端适配。
- 局外存档、Boss Rush、解锁。

**总工时估算：4~5 个工作日。**

---

## 十、关键代码示例

### 10.1 弹幕模式实现示例
```javascript
function firePattern(boss, pattern, dt) {
  boss.patternTimer += dt;
  switch (pattern) {
    case 'ring': {
      const n = 24, speed = 180;
      for (let i = 0; i < n; i++) {
        const a = (i / n) * Math.PI * 2 + boss.patternTimer;
        spawnBullet(boss.x, boss.y, Math.cos(a)*speed, Math.sin(a)*speed, 'enemy');
      }
      return 0.8;  // 下次发射间隔
    }
    case 'spiral': {
      const a = boss.patternTimer * 8;
      spawnBullet(boss.x, boss.y, Math.cos(a)*200, Math.sin(a)*200, 'enemy');
      return 0.05;
    }
    case 'aimed_spread': {
      const base = angleTo(boss, player);
      for (let i = -2; i <= 2; i++) {
        const a = base + i * 0.15;
        spawnBullet(boss.x, boss.y, Math.cos(a)*240, Math.sin(a)*240, 'enemy');
      }
      return 0.6;
    }
  }
}
```

### 10.2 擦弹判定示例
```javascript
function checkGraze(player, bullets) {
  for (const b of bullets) {
    if (b.grazed) continue;
    const d = dist(player.x, player.y, b.x, b.y);
    if (d < player.grazeWindow && d > player.r + b.r) {
      b.grazed = true;
      player.empCharge = Math.min(1, player.empCharge + 0.01);
      addScore(50);
      flashEdge();
      playSfx('graze');
    }
  }
}
```

### 10.3 程序化战机绘制示例
```javascript
function drawPlayer(ctx, p) {
  ctx.save();
  ctx.translate(p.x, p.y);
  ctx.shadowColor = '#00f0ff';
  ctx.shadowBlur = 18;
  ctx.fillStyle = '#0af';
  ctx.beginPath();
  ctx.moveTo(0, -18);          // 机鼻
  ctx.lineTo(12, 10);
  ctx.lineTo(6, 6);
  ctx.lineTo(0, 12);
  ctx.lineTo(-6, 6);
  ctx.lineTo(-12, 10);
  ctx.closePath();
  ctx.fill();
  // 引擎光
  ctx.fillStyle = '#fff';
  ctx.fillRect(-3, 10, 6, 4);
  ctx.restore();

  // 精准模式下显示判定点
  if (p.focused) {
    ctx.fillStyle = '#ff2a4d';
    ctx.fillRect(p.x - 1, p.y - 1, 3, 3);
  }
}
```

---

## 十一、验收标准

### 11.1 功能验收
- [ ] 三章关卡全部可通关，三章末各有一场 Boss 战。
- [ ] 玩家可切换 3 种主武器，效果明显不同。
- [ ] 大招、护盾、炸弹、擦弹、连击全部生效。
- [ ] 三种难度可选，数值差异可感知。
- [ ] 死亡后可重开，分数与解锁存档正确。
- [ ] Boss Rush 通关后解锁。
- [ ] 移动端虚拟摇杆可玩。

### 11.2 性能验收
- [ ] 桌面 Chrome 60 FPS 稳定（同屏 600 弹 + 800 粒子时）。
- [ ] 首屏加载 < 2 秒（不含字体）。
- [ ] 无内存泄漏（连续游玩 10 分钟内存稳定）。

### 11.3 视觉验收
- [ ] 全程保持科技霓虹风格，色彩统一。
- [ ] 命中/爆炸/擦弹均有明显视觉反馈。
- [ ] Boss 入场/破阶段/死亡有特效与震屏。
- [ ] HUD 信息清晰，不遮挡战斗。

### 11.4 交互验收
- [ ] 键盘（WASD/方向键 + Space + Shift）+ 鼠标 + 触屏均可操作。
- [ ] 暂停/继续/重开/退出菜单可用。
- [ ] 音量与静音可调并持久化。

---

## 十二、待你确认/可优化项

请在以下点上给出偏好，Builder 会据此实现：

1. **战机数量**：是否需要多架可选战机（不同基础属性），还是单战机多武器？（默认：单战机 + 3 武器）
2. **存档范围**：是否需要账号/云存档？（默认：仅 localStorage）
3. **音效策略**：是否接受全合成音效，还是需要预留外部音效文件接口？（默认：全合成）
4. **Boss Rush**：是否在 MVP 中实现，还是放在后续迭代？（默认：后续迭代）
5. **分享/排行**：是否需要本地排行榜或分享分数功能？（默认：仅本地最高分）
6. **多语言**：是否需要中英双语切换？（默认：仅中文）
7. **代码组织**：单文件交付 vs 多文件模块化？（默认：多文件，可合并）

---

**文档结束。请在本文档基础上直接修改/批注，确认后交由 Builder 实现。**
